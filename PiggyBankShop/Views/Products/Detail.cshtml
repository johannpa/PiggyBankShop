@model PiggyBankShop.Models.Product

<!DOCTYPE html>

<html>
    <head>
        <title>Piggy Bank Shop</title>
    </head>
    <body>
        <img src="@Model.ImageUrl"/>
        <h3>@Model.Name</h3>
        <p>@Model.Detail</p>
        <h5>@Model.Price.ToString("c")</h5>

    </body>
</html>
